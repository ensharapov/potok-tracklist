# ПОТОК — Интерактивный трекер практик

Telegram WebApp для отслеживания прогресса по модулям программы "Поток".

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
pnpm install

# Запуск dev-сервера
pnpm run dev

# Сборка для production
pnpm build
```

### Деплой на Vercel

1. Подключите репозиторий к Vercel через GitHub
2. Vercel автоматически определит настройки из `vercel.json`
3. После деплоя получите HTTPS URL для Telegram WebApp

## 📱 Интеграция с Telegram

1. В BotFather создайте кнопку с типом `web_app`:
   ```
   /newbot -> создайте бота
   /setdomain -> укажите домен Vercel (например: your-app.vercel.app)
   ```

2. Добавьте кнопку в меню бота:
   ```json
   {
     "type": "web_app",
     "text": "Открыть Поток",
     "web_app": {
       "url": "https://your-app.vercel.app"
     }
   }
   ```

## 🛠 Технологии

- **Frontend**: React + TypeScript + Vite
- **UI**: Tailwind CSS + Radix UI
- **Telegram**: WebApp SDK
- **Deploy**: Vercel

## 📝 Структура проекта

```
├── client/          # React приложение
├── server/          # Express сервер (опционально)
├── shared/          # Общие константы
└── dist/            # Собранные файлы
```

## 🔐 Переменные окружения

Для локальной разработки создайте `.env.local` (не коммитится в git).

## 📄 Лицензия

MIT

