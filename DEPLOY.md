# 🚀 Инструкция по деплою

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Название репозитория: `potok-interactive-tracklist` (или любое другое)
3. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже всё есть)
4. Нажмите "Create repository"

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет команды. Выполните их в терминале:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/potok-interactive-tracklist.git
git branch -M main
git push -u origin main
```

**Или если используете SSH:**
```bash
git remote add origin git@github.com:YOUR_USERNAME/potok-interactive-tracklist.git
git branch -M main
git push -u origin main
```

## Шаг 3: Подключите проект к Vercel

1. Откройте https://vercel.com
2. Нажмите "Add New..." → "Project"
3. Импортируйте ваш GitHub репозиторий
4. Vercel автоматически определит настройки из `vercel.json`
5. Нажмите "Deploy"

## Шаг 4: Получите URL и обновите бота

После деплоя Vercel даст вам URL вида: `https://your-app.vercel.app`

1. Скопируйте этот URL
2. В вашем Telegram боте обновите ссылку на WebApp на новый URL

## Шаг 5: Настройте домен в BotFather (если нужно)

Если BotFather требует подтверждения домена:

1. В BotFather выполните: `/setdomain`
2. Укажите домен: `your-app.vercel.app` (без https://)
3. Vercel автоматически предоставит HTTPS

## ✅ Готово!

Теперь ваш WebApp доступен по HTTPS и готов к использованию в Telegram!

---

## 🔄 Обновление проекта

После любых изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически пересоберёт и задеплоит новую версию.

