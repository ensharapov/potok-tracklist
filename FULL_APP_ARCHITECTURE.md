# Архитектура полноценного приложения (iOS + Android + Web)

## 🎯 Текущее состояние

**Что у нас есть:**
- ✅ React приложение (Vite)
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ Radix UI компоненты
- ✅ Telegram WebApp интеграция
- ✅ Supabase (PostgreSQL)
- ✅ Serverless API (Vercel Functions)

**Что нужно добавить:**
- 📱 iOS приложение
- 🤖 Android приложение
- 🌐 Веб-сайт (отдельно от Telegram Mini App)
- 🔐 Авторизация (не только через Telegram)
- 📲 Push-уведомления
- 💾 Офлайн режим
- 📸 Нативные функции (камера, геолокация и т.д.)

---

## 🏗️ Варианты архитектуры

### Вариант 1: React Native + React Native Web (РЕКОМЕНДУЮ) ⭐

**Стек:**
- **React Native** (iOS + Android)
- **React Native Web** (веб-сайт)
- **Expo** (упрощает разработку)
- **Общий backend API** (текущий Supabase)
- **TypeScript** (общий для всех платформ)

**Структура проекта:**
```
potok-app/
├── packages/
│   ├── shared/              # Общая бизнес-логика
│   │   ├── types/           # TypeScript типы
│   │   ├── hooks/           # React hooks
│   │   ├── utils/           # Утилиты
│   │   └── api/             # API клиент
│   ├── mobile/              # React Native (iOS + Android)
│   │   ├── src/
│   │   │   ├── screens/      # Экраны приложения
│   │   │   ├── components/  # Нативные компоненты
│   │   │   └── navigation/  # Навигация
│   │   └── app.json         # Expo конфиг
│   └── web/                 # React Native Web
│       ├── src/
│       │   ├── pages/       # Страницы
│       │   └── components/ # Веб-компоненты
│       └── index.html
├── server/                  # Backend API (общий)
│   ├── api/                # Serverless functions
│   └── supabase/           # Supabase интеграция
└── package.json            # Monorepo root
```

**Что можно переиспользовать:**
- ✅ **80-90% бизнес-логики** (типы, хуки, утилиты)
- ✅ **API клиент** (axios запросы)
- ✅ **Состояние** (React hooks)
- ✅ **Backend** (Supabase, API endpoints)

**Что нужно переписать:**
- ⚠️ **UI компоненты** (Radix UI → React Native компоненты)
- ⚠️ **Стили** (Tailwind CSS → StyleSheet или styled-components)
- ⚠️ **Навигация** (wouter → React Navigation)
- ⚠️ **Telegram интеграция** (только для веб-версии или через Telegram SDK)

**Плюсы:**
- ✅ Один код для всех платформ
- ✅ Можно переиспользовать большую часть логики
- ✅ Expo упрощает деплой и разработку
- ✅ TypeScript везде
- ✅ Быстрая разработка

**Минусы:**
- ⚠️ Нужно переписать UI компоненты
- ⚠️ Некоторые нативные функции требуют дополнительных библиотек

---

### Вариант 2: Flutter (Dart)

**Стек:**
- **Flutter** (iOS + Android + Web)
- **Dart** язык
- **Общий backend API**

**Что нужно переписать:**
- ❌ **Всё с нуля** (другой язык, другой фреймворк)
- ❌ **Вся бизнес-логика**
- ❌ **Все компоненты**

**Плюсы:**
- ✅ Отличная производительность
- ✅ Красивый UI из коробки
- ✅ Один код для всех платформ

**Минусы:**
- ❌ Нужно переписывать всё с нуля
- ❌ Другой язык (Dart)
- ❌ Нельзя переиспользовать текущий код

**Вывод:** Не рекомендую, если уже есть React код.

---

### Вариант 3: Native (Swift + Kotlin)

**Стек:**
- **Swift** (iOS)
- **Kotlin** (Android)
- **React** (веб, текущий)

**Что нужно переписать:**
- ❌ **iOS приложение** (Swift)
- ❌ **Android приложение** (Kotlin)
- ✅ **Веб** (остаётся React)

**Плюсы:**
- ✅ Максимальная производительность
- ✅ Полный доступ к нативным функциям
- ✅ Лучший UX

**Минусы:**
- ❌ Нужно писать два приложения
- ❌ Нельзя переиспользовать код
- ❌ Дольше разработка
- ❌ Нужны два разработчика (iOS + Android)

**Вывод:** Только если нужна максимальная производительность и есть ресурсы.

---

### Вариант 4: PWA (Progressive Web App)

**Стек:**
- **Текущий React** приложение
- **Service Workers** для офлайн режима
- **Web App Manifest** для установки

**Что нужно добавить:**
- ✅ Service Worker
- ✅ Web App Manifest
- ✅ Офлайн кеширование
- ✅ Push-уведомления (через браузер)

**Плюсы:**
- ✅ Можно использовать текущий код
- ✅ Работает на всех платформах
- ✅ Можно установить на домашний экран

**Минусы:**
- ⚠️ Ограниченный доступ к нативным функциям
- ⚠️ Нельзя опубликовать в App Store / Google Play как нативное приложение
- ⚠️ Push-уведомления работают только в браузере

**Вывод:** Хороший вариант для быстрого старта, но не полноценная замена нативным приложениям.

---

## 🎯 Рекомендация: React Native + Expo

### Почему этот вариант?

1. **Максимальное переиспользование кода**
   - Бизнес-логика (80-90%)
   - API клиент
   - TypeScript типы
   - React hooks

2. **Быстрая разработка**
   - Expo упрощает деплой
   - Hot reload
   - Один код для всех платформ

3. **Гибкость**
   - Можно добавить нативные модули при необходимости
   - Можно использовать существующие библиотеки

4. **Сообщество**
   - Большая экосистема
   - Много готовых решений

---

## 📐 Детальная архитектура (React Native + Expo)

### Структура проекта (Monorepo)

```
potok-app/
├── packages/
│   ├── shared/                    # Общий код
│   │   ├── src/
│   │   │   ├── types/
│   │   │   │   ├── practice.ts    # Типы практик
│   │   │   │   ├── user.ts        # Типы пользователя
│   │   │   │   └── progress.ts    # Типы прогресса
│   │   │   ├── hooks/
│   │   │   │   ├── useProgress.ts # Хук для прогресса
│   │   │   │   └── useAuth.ts     # Хук для авторизации
│   │   │   ├── api/
│   │   │   │   ├── client.ts      # API клиент (axios)
│   │   │   │   ├── progress.ts    # API для прогресса
│   │   │   │   └── auth.ts        # API для авторизации
│   │   │   └── utils/
│   │   │       ├── storage.ts     # Утилиты для localStorage
│   │   │       └── calculations.ts # Расчёты прогресса
│   │   └── package.json
│   │
│   ├── mobile/                    # React Native (iOS + Android)
│   │   ├── src/
│   │   │   ├── screens/
│   │   │   │   ├── HomeScreen.tsx      # Главный экран
│   │   │   │   ├── ModuleScreen.tsx    # Экран модуля
│   │   │   │   ├── PracticeScreen.tsx  # Экран практики
│   │   │   │   └── ProfileScreen.tsx   # Профиль
│   │   │   ├── components/
│   │   │   │   ├── PracticeItem.tsx    # Компонент практики
│   │   │   │   ├── ProgressBar.tsx    # Прогресс-бар
│   │   │   │   └── ModuleCard.tsx     # Карточка модуля
│   │   │   ├── navigation/
│   │   │   │   └── AppNavigator.tsx    # Навигация
│   │   │   └── App.tsx
│   │   ├── app.json                 # Expo конфиг
│   │   └── package.json
│   │
│   └── web/                       # React Native Web
│       ├── src/
│       │   ├── pages/
│       │   │   ├── HomePage.tsx
│       │   │   └── ModulePage.tsx
│       │   ├── components/
│       │   │   └── ...
│       │   └── App.tsx
│       ├── index.html
│       └── package.json
│
├── server/                        # Backend (общий)
│   ├── api/
│   │   ├── progress-supabase.ts   # API для прогресса
│   │   └── auth.ts                # API для авторизации
│   └── package.json
│
├── package.json                   # Monorepo root
├── pnpm-workspace.yaml            # pnpm workspace
└── tsconfig.json                   # Общий TypeScript конфиг
```

---

## 🔄 План миграции

### Этап 1: Подготовка (1-2 недели)

1. **Настроить Monorepo**
   ```bash
   # Создать структуру
   mkdir -p packages/{shared,mobile,web}
   
   # Настроить pnpm workspace
   # Настроить TypeScript paths
   ```

2. **Вынести общий код**
   - Типы (`Practice`, `User`, `Progress`)
   - Хуки (`useProgress`, `useAuth`)
   - API клиент
   - Утилиты

3. **Настроить React Native проект**
   ```bash
   npx create-expo-app@latest packages/mobile
   ```

### Этап 2: Мобильное приложение (3-4 недели)

1. **Создать базовые экраны**
   - HomeScreen (главный экран)
   - ModuleScreen (экран модуля)
   - PracticeScreen (экран практики)

2. **Адаптировать компоненты**
   - Заменить Radix UI на React Native компоненты
   - Адаптировать стили (Tailwind → StyleSheet)

3. **Настроить навигацию**
   - React Navigation
   - Навигация между экранами

4. **Интегрировать API**
   - Использовать общий API клиент из `shared`
   - Подключить Supabase

### Этап 3: Веб-версия (1-2 недели)

1. **Настроить React Native Web**
   ```bash
   npm install react-native-web
   ```

2. **Адаптировать компоненты**
   - Использовать те же компоненты, что и в мобильном
   - Добавить веб-специфичные стили

3. **Настроить роутинг**
   - React Router или React Navigation Web

### Этап 4: Дополнительные функции (2-3 недели)

1. **Авторизация**
   - Telegram (только для веба)
   - Email/Password
   - OAuth (Google, Apple)

2. **Push-уведомления**
   - Expo Notifications
   - Firebase Cloud Messaging (для Android)

3. **Офлайн режим**
   - AsyncStorage для локального хранения
   - Синхронизация при подключении

4. **Нативные функции**
   - Камера (для фото практик)
   - Геолокация (если нужно)
   - Календарь (для напоминаний)

---

## 🛠️ Технологический стек

### Frontend (Mobile + Web)

```json
{
  "dependencies": {
    "react": "^18.3.1",
    "react-native": "^0.73.0",
    "react-native-web": "^0.19.0",
    "expo": "~50.0.0",
    "@react-navigation/native": "^6.1.0",
    "@react-navigation/stack": "^6.3.0",
    "@supabase/supabase-js": "^2.86.0",
    "axios": "^1.12.0",
    "zustand": "^4.4.0",  // State management
    "react-query": "^5.0.0",  // Data fetching
    "expo-notifications": "~0.27.0",  // Push notifications
    "expo-camera": "~14.0.0",  // Камера
    "@react-native-async-storage/async-storage": "^1.21.0"  // Локальное хранилище
  }
}
```

### Backend (остаётся как есть)

- Supabase (PostgreSQL)
- Serverless Functions (Vercel или Yandex Cloud)
- API endpoints

---

## 📱 Что будет в каждом приложении

### iOS / Android (React Native)

**Экраны:**
- 🏠 Главный экран (список модулей, прогресс)
- 📚 Экран модуля (список практик)
- ✅ Экран практики (детали, чекбокс)
- 📊 Статистика (прогресс, графики)
- 👤 Профиль (настройки, авторизация)
- 🔔 Уведомления (напоминания о практиках)

**Функции:**
- ✅ Отслеживание прогресса
- ✅ Офлайн режим
- ✅ Push-уведомления
- ✅ Камера (для фото практик)
- ✅ Календарь (напоминания)

### Web (React Native Web)

**Страницы:**
- 🏠 Главная (то же, что и мобильное)
- 📚 Модули
- ✅ Практики
- 📊 Статистика
- 👤 Профиль

**Функции:**
- ✅ Отслеживание прогресса
- ✅ Telegram интеграция (только для веба)
- ✅ PWA (можно установить)
- ✅ Офлайн режим (Service Worker)

---

## 🔐 Авторизация

### Варианты:

1. **Telegram** (только для веба)
   - Текущая интеграция
   - Работает только в Telegram WebApp

2. **Email/Password**
   - Supabase Auth
   - Работает везде

3. **OAuth**
   - Google Sign-In
   - Apple Sign-In (iOS)
   - Работает везде

4. **Гибридный подход**
   - Telegram для веба (если открыто в Telegram)
   - Email/Password для мобильных и веба
   - OAuth как альтернатива

---

## 📊 Сравнение вариантов

| Критерий | React Native | Flutter | Native | PWA |
|----------|-------------|---------|--------|-----|
| Переиспользование кода | ✅ 80-90% | ❌ 0% | ❌ 0% | ✅ 100% |
| Скорость разработки | ✅ Быстро | ⚠️ Средне | ❌ Медленно | ✅ Очень быстро |
| Производительность | ✅ Хорошая | ✅ Отличная | ✅ Отличная | ⚠️ Средняя |
| Доступ к нативным функциям | ✅ Да | ✅ Да | ✅ Да | ⚠️ Ограничен |
| Публикация в App Store | ✅ Да | ✅ Да | ✅ Да | ❌ Нет |
| Стоимость разработки | ✅ Низкая | ⚠️ Средняя | ❌ Высокая | ✅ Очень низкая |

---

## 🎯 Итоговая рекомендация

### Для вашего случая: **React Native + Expo**

**Почему:**
1. ✅ Можно переиспользовать 80-90% текущего кода
2. ✅ Быстрая разработка
3. ✅ Один код для всех платформ
4. ✅ Можно опубликовать в App Store и Google Play
5. ✅ Хорошая производительность
6. ✅ Большая экосистема

**План действий:**
1. Начать с мобильного приложения (iOS + Android)
2. Затем добавить веб-версию (React Native Web)
3. Постепенно добавлять функции (push, офлайн, камера)

**Временные затраты:**
- Подготовка: 1-2 недели
- Мобильное приложение: 3-4 недели
- Веб-версия: 1-2 недели
- Дополнительные функции: 2-3 недели
- **Итого: 7-11 недель**

---

## ❓ Вопросы для уточнения

1. **Нужна ли публикация в App Store / Google Play?**
   - Если да → React Native или Native
   - Если нет → можно PWA

2. **Какие нативные функции нужны?**
   - Камера, геолокация, календарь, push-уведомления?

3. **Нужна ли авторизация через Telegram?**
   - Только для веба или везде?

4. **Какой бюджет и сроки?**
   - React Native быстрее и дешевле
   - Native дороже, но лучше производительность

---

## 🚀 Следующие шаги

Если решите идти с React Native:

1. Я помогу настроить Monorepo
2. Вынести общий код в `shared`
3. Создать базовую структуру мобильного приложения
4. Адаптировать текущие компоненты под React Native
5. Настроить навигацию и API

**Готов начать, когда скажете!** 🎉




