# Миграция полноценного приложения на Яндекс.Облако + RuStore

## 🎯 Обзор

Для полноценного приложения (iOS + Android + Web) на инфраструктуре Яндекс можно перенести:

### ✅ Что можно перенести на Яндекс

1. **Backend API** → Yandex Cloud Functions
2. **База данных** → Yandex Managed PostgreSQL
3. **Статические файлы** → Yandex Object Storage + CDN
4. **Push-уведомления** → Yandex Cloud Messaging (или Firebase альтернатива)
5. **CI/CD** → Yandex Cloud Build
6. **Мониторинг** → Yandex Monitoring
7. **Аналитика** → Yandex Metrica / AppMetrica

### ❌ Что нельзя заменить

- **App Store (iOS)** — только Apple
- **Google Play** — только Google (но можно параллельно с RuStore)
- **Telegram WebApp SDK** — официальный SDK
- **React Native / Expo** — инструменты разработки

---

## 📦 Детальный план миграции на Яндекс

### 1. Backend API → Yandex Cloud Functions

**Текущая инфраструктура:**
- Vercel Serverless Functions (`/api/*`)
- Node.js runtime

**Замена на Яндекс:**
- **Yandex Cloud Functions** — serverless функции
- Node.js 18+ runtime
- HTTP триггеры

**Что нужно изменить:**

#### Текущий формат (Vercel):
```typescript
// api/progress-supabase.ts
import type { VercelRequest, VercelResponse } from '@vercel/node';

export default async function handler(
  req: VercelRequest,
  res: VercelResponse
) {
  if (req.method === 'POST') {
    // ...
    res.json({ success: true });
  }
}
```

#### Новый формат (Yandex Functions):
```typescript
// api/progress-supabase.ts
import { Handler } from '@yandex-cloud/function-types';

export const handler: Handler.Http = async (event, context) => {
  const { httpMethod, body, queryStringParameters } = event;
  
  // CORS headers
  const headers = {
    'Content-Type': 'application/json',
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
    'Access-Control-Allow-Headers': 'Content-Type',
  };
  
  if (httpMethod === 'OPTIONS') {
    return {
      statusCode: 200,
      headers,
      body: '',
    };
  }
  
  if (httpMethod === 'POST') {
    const { userId, checkedItems, appMode } = JSON.parse(body || '{}');
    
    // Логика сохранения в PostgreSQL
    // ...
    
    return {
      statusCode: 200,
      headers,
      body: JSON.stringify({ success: true }),
    };
  }
  
  if (httpMethod === 'GET') {
    const userId = queryStringParameters?.userId;
    
    // Логика загрузки из PostgreSQL
    // ...
    
    return {
      statusCode: 200,
      headers,
      body: JSON.stringify({ checkedItems: {}, appMode: null }),
    };
  }
  
  return {
    statusCode: 405,
    headers,
    body: JSON.stringify({ error: 'Method not allowed' }),
  };
};
```

**Деплой:**
```bash
# Установка Yandex CLI
curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash

# Создание функции
yc serverless function create --name progress-api

# Деплой
yc serverless function version create \
  --function-name progress-api \
  --runtime nodejs18 \
  --entrypoint handler \
  --source-path . \
  --environment SUPABASE_URL=...,SUPABASE_KEY=...
```

---

### 2. База данных → Yandex Managed PostgreSQL

**Текущая инфраструктура:**
- Supabase PostgreSQL
- REST API через Supabase

**Замена на Яндекс:**
- **Yandex Managed PostgreSQL** — полностью совместим
- Прямое подключение через `pg` драйвер
- Или через REST API (если нужен)

**Что нужно изменить:**

#### Обновление подключения:
```typescript
// server/lib/db.ts
import { Pool } from 'pg';

const pool = new Pool({
  host: process.env.YANDEX_PG_HOST,
  port: parseInt(process.env.YANDEX_PG_PORT || '5432'),
  database: process.env.YANDEX_PG_DATABASE,
  user: process.env.YANDEX_PG_USER,
  password: process.env.YANDEX_PG_PASSWORD,
  ssl: {
    rejectUnauthorized: false, // Для Yandex Managed PostgreSQL
  },
});

export { pool };
```

#### Обновление API:
```typescript
// api/progress-supabase.ts (переименовать в progress-pg.ts)
import { pool } from '../lib/db';

export const handler: Handler.Http = async (event, context) => {
  if (event.httpMethod === 'POST') {
    const { userId, checkedItems, appMode } = JSON.parse(event.body || '{}');
    
    // Сохранение в Yandex PostgreSQL
    const client = await pool.connect();
    try {
      await client.query(
        `INSERT INTO user_progress (user_id, checked_items, updated_at)
         VALUES ($1, $2, NOW())
         ON CONFLICT (user_id) 
         DO UPDATE SET checked_items = $2, updated_at = NOW()`,
        [userId, JSON.stringify(checkedItems)]
      );
    } finally {
      client.release();
    }
    
    return {
      statusCode: 200,
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ success: true }),
    };
  }
  
  // GET запрос аналогично
};
```

**Миграция данных:**
```bash
# Экспорт из Supabase
pg_dump -h <supabase-host> -U <user> -d <database> > backup.sql

# Импорт в Yandex PostgreSQL
psql -h <yandex-pg-host> -U <user> -d <database> < backup.sql
```

---

### 3. Статические файлы → Yandex Object Storage + CDN

**Текущая инфраструктура:**
- Vercel Static Hosting
- Автоматический CDN

**Замена на Яндекс:**
- **Yandex Object Storage** (S3-совместимое)
- **Yandex CDN** для раздачи статики
- **Application Load Balancer** для маршрутизации

**Настройка:**

```bash
# Создание bucket
yc storage bucket create --name potok-app-static

# Загрузка статики
yc storage cp -r dist/public/* s3://potok-app-static/

# Настройка CDN
yc cdn resource create \
  --origin-source potok-app-static.storage.yandexcloud.net \
  --origin-protocol https
```

**Обновление клиента:**
```typescript
// client/src/lib/config.ts
export const API_URL = import.meta.env.VITE_API_URL || 
  'https://functions.yandexcloud.net/<function-id>';
```

---

### 4. Push-уведомления → Yandex Cloud Messaging / Firebase

**Для мобильных приложений:**

#### Вариант A: Firebase Cloud Messaging (FCM)
- Работает на Яндекс.Облаке (если настроен)
- Или можно использовать напрямую Firebase

#### Вариант B: Yandex Cloud Messaging
- Если Яндекс предоставляет альтернативу
- Или использовать сторонние сервисы (OneSignal, Pusher)

**Настройка для React Native:**
```typescript
// packages/mobile/src/services/notifications.ts
import * as Notifications from 'expo-notifications';
import { Platform } from 'react-native';

export async function registerForPushNotifications() {
  if (Platform.OS === 'android') {
    await Notifications.setNotificationChannelAsync('default', {
      name: 'default',
      importance: Notifications.AndroidImportance.MAX,
    });
  }

  const { status } = await Notifications.requestPermissionsAsync();
  if (status !== 'granted') {
    return null;
  }

  const token = await Notifications.getExpoPushTokenAsync();
  return token.data;
}
```

---

### 5. CI/CD → Yandex Cloud Build

**Текущая инфраструктура:**
- GitHub Actions (предположительно)
- Автоматический деплой на Vercel

**Замена на Яндекс:**
- **Yandex Cloud Build** — CI/CD сервис
- Или **GitLab CI** (если используете GitLab)

**Пример `.yandex/build.yaml`:**
```yaml
version: '1.0'
steps:
  - name: build
    image: node:18
    commands:
      - pnpm install
      - pnpm build
    artifacts:
      - dist/
  
  - name: deploy-functions
    image: yandex/yc
    commands:
      - yc serverless function version create --function-name progress-api --source-path dist/api
  
  - name: deploy-static
    image: yandex/yc
    commands:
      - yc storage cp -r dist/public/* s3://potok-app-static/
```

---

### 6. Мониторинг → Yandex Monitoring

**Текущая инфраструктура:**
- Vercel Analytics
- Возможно, сторонние сервисы

**Замена на Яндекс:**
- **Yandex Monitoring** — метрики и алерты
- **Yandex Application Logging** — логи

**Настройка:**
```typescript
// server/lib/monitoring.ts
import { createLogger } from '@yandex-cloud/logging';

const logger = createLogger({
  service: 'potok-api',
  level: 'info',
});

export { logger };
```

---

## 📱 RuStore: Параллельная разработка

### Что такое RuStore?

**RuStore** — российский магазин приложений для Android, разработанный VK при поддержке Минцифры РФ.

**Особенности:**
- ✅ Альтернатива Google Play для российского рынка
- ✅ Поддержка Android приложений
- ✅ Монетизация (платные приложения, подписки, IAP)
- ✅ Аналитика и продвижение
- ✅ Требования соответствия российскому законодательству

---

### Требования для публикации в RuStore

#### 1. Технические требования

**Формат приложения:**
- ✅ APK или AAB (Android App Bundle)
- ✅ Минимальная версия Android: 5.0 (API 21)
- ✅ Целевая версия Android: рекомендуется 13+ (API 33)

**Подписи:**
- ✅ Приложение должно быть подписано
- ✅ Использовать один и тот же ключ для всех обновлений

**Разрешения:**
- ✅ Запрашивать только необходимые разрешения
- ✅ Объяснять, зачем нужны разрешения

#### 2. Контентные требования

- ✅ Соответствие российскому законодательству
- ✅ Возрастные ограничения (18+, 12+ и т.д.)
- ✅ Отсутствие запрещённого контента
- ✅ Политика конфиденциальности на русском языке

#### 3. Юридические требования

- ✅ Регистрация разработчика (ИП или ООО)
- ✅ Согласие с правилами RuStore
- ✅ Политика конфиденциальности
- ✅ Пользовательское соглашение

---

### План разработки для RuStore

#### Этап 1: Подготовка (1 неделя)

1. **Регистрация в RuStore**
   - Зарегистрироваться как разработчик
   - Подтвердить аккаунт
   - Заполнить профиль

2. **Настройка проекта**
   - Убедиться, что React Native проект собирается в APK/AAB
   - Настроить подпись приложения
   - Проверить минимальную версию Android

#### Этап 2: Адаптация приложения (1-2 недели)

1. **Локализация**
   - Убедиться, что все тексты на русском
   - Добавить поддержку других языков (если нужно)

2. **Соответствие требованиям**
   - Проверить разрешения
   - Добавить политику конфиденциальности
   - Настроить возрастные ограничения

3. **Монетизация (если нужно)**
   - Интегрировать RuStore SDK для покупок
   - Настроить подписки (если есть)

#### Этап 3: Тестирование (1 неделя)

1. **Внутреннее тестирование**
   - Установить APK на устройства
   - Проверить все функции
   - Исправить баги

2. **Бета-тестирование**
   - Выпустить закрытую бета-версию
   - Собрать отзывы
   - Исправить проблемы

#### Этап 4: Публикация (1 неделя)

1. **Подготовка к публикации**
   - Создать описание приложения
   - Подготовить скриншоты (минимум 2, максимум 8)
   - Подготовить иконку (512x512)
   - Подготовить промо-изображение (1024x500)

2. **Загрузка в RuStore**
   - Загрузить APK/AAB
   - Заполнить метаданные
   - Отправить на модерацию

3. **Модерация**
   - Ожидать проверки (обычно 1-3 дня)
   - Исправить замечания (если есть)
   - Получить одобрение

---

### Интеграция RuStore SDK

**Установка:**
```bash
npm install @rustore/sdk
```

**Использование:**
```typescript
// packages/mobile/src/services/rustore.ts
import { RuStoreSDK } from '@rustore/sdk';

// Инициализация
const rustore = new RuStoreSDK({
  apiKey: 'your-api-key',
});

// Проверка покупок
export async function checkPurchases() {
  const purchases = await rustore.getPurchases();
  return purchases;
}

// Покупка подписки
export async function purchaseSubscription(productId: string) {
  const result = await rustore.purchase(productId);
  return result;
}
```

---

### Параллельная публикация: Google Play + RuStore

**Стратегия:**

1. **Один код, два магазина**
   - Один React Native проект
   - Разные конфигурации для сборки
   - Разные ключи подписи (опционально)

2. **Настройка сборки:**
```json
// packages/mobile/app.json
{
  "expo": {
    "android": {
      "package": "com.potok.app",
      "versionCode": 1,
      "version": "1.0.0",
      "adaptiveIcon": {
        "foregroundImage": "./assets/icon.png"
      }
    }
  }
}
```

3. **Скрипты сборки:**
```json
// package.json
{
  "scripts": {
    "build:android:play": "eas build --platform android --profile production",
    "build:android:rustore": "eas build --platform android --profile rustore"
  }
}
```

4. **Разные профили:**
```yaml
# eas.json
{
  "build": {
    "production": {
      "android": {
        "buildType": "apk",
        "gradleCommand": ":app:assembleRelease"
      }
    },
    "rustore": {
      "android": {
        "buildType": "aab",
        "gradleCommand": ":app:bundleRelease"
      }
    }
  }
}
```

---

## 🏗️ Итоговая архитектура на Яндекс

```
┌─────────────────────────────────────────────────────────┐
│                    Пользователи                          │
│  iOS App  │  Android App  │  Web App  │  Telegram      │
└───────────┴───────────────┴────────────┴────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│              Yandex Application Load Balancer            │
│                    (Маршрутизация)                       │
└─────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Yandex     │    │   Yandex     │    │   Yandex     │
│   Object     │    │   Cloud      │    │   Managed    │
│   Storage    │    │   Functions   │    │   PostgreSQL │
│  (Статика)   │    │   (API)      │    │   (БД)       │
└──────────────┘    └──────────────┘    └──────────────┘
         │                    │                    │
         └────────────────────┴────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Yandex CDN     │
                    │  (Кеширование)  │
                    └──────────────────┘
```

---

## 📊 Сравнение: Vercel/Supabase vs Yandex Cloud

| Компонент | Текущее | Яндекс | Сложность миграции |
|-----------|---------|--------|-------------------|
| **API** | Vercel Functions | Yandex Functions | ⚠️ Средняя (изменить формат handler) |
| **База данных** | Supabase | Yandex PostgreSQL | ✅ Легкая (та же PostgreSQL) |
| **Статика** | Vercel Hosting | Object Storage + CDN | ⚠️ Средняя (настроить CDN) |
| **Push** | Firebase | Yandex/Firebase | ✅ Легкая (та же интеграция) |
| **CI/CD** | GitHub Actions | Yandex Build | ⚠️ Средняя (новый формат) |
| **Мониторинг** | Vercel Analytics | Yandex Monitoring | ⚠️ Средняя (новый сервис) |

---

## 🎯 План действий

### Фаза 1: Миграция Backend (2-3 недели)

1. **Настроить Yandex Cloud**
   - Создать аккаунт
   - Создать Managed PostgreSQL
   - Создать Cloud Functions

2. **Мигрировать API**
   - Адаптировать функции под Yandex формат
   - Протестировать endpoints
   - Настроить переменные окружения

3. **Мигрировать базу данных**
   - Экспортировать данные из Supabase
   - Импортировать в Yandex PostgreSQL
   - Обновить подключения

### Фаза 2: Миграция Frontend (1-2 недели)

1. **Настроить Object Storage**
   - Создать bucket
   - Настроить CDN
   - Загрузить статику

2. **Обновить клиент**
   - Изменить API URLs
   - Протестировать подключение
   - Обновить переменные окружения

### Фаза 3: RuStore (параллельно, 2-3 недели)

1. **Подготовка**
   - Регистрация в RuStore
   - Настройка проекта
   - Локализация

2. **Разработка**
   - Адаптация под требования
   - Интеграция SDK (если нужно)
   - Тестирование

3. **Публикация**
   - Подготовка материалов
   - Загрузка в RuStore
   - Модерация

---

## ✅ Преимущества миграции на Яндекс

1. **Соответствие требованиям**
   - Данные хранятся в России
   - Соответствие 152-ФЗ

2. **Производительность**
   - Низкая задержка для российских пользователей
   - Быстрая загрузка

3. **Стоимость**
   - Конкурентные цены
   - Гибкие тарифы

4. **Поддержка**
   - Русскоязычная поддержка
   - Локальная документация

---

## ❓ Вопросы для уточнения

1. **Нужна ли полная миграция или гибридный подход?**
   - Можно оставить Supabase, заменить только Vercel

2. **Какие функции критичны?**
   - Push-уведомления, аналитика, офлайн режим?

3. **Бюджет и сроки?**
   - Миграция займёт 4-6 недель
   - RuStore разработка: 2-3 недели

4. **Нужна ли монетизация в RuStore?**
   - Платные функции, подписки?

---

## 🚀 Готов начать!

Если решите мигрировать, я помогу:
1. ✅ Адаптировать API под Yandex Functions
2. ✅ Настроить подключение к Yandex PostgreSQL
3. ✅ Настроить деплой статики
4. ✅ Подготовить приложение для RuStore
5. ✅ Настроить CI/CD

**Главное:** Работа в Cursor **НЕ ИЗМЕНИТСЯ** — мы продолжим работать точно так же! 🎉




